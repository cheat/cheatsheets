---
tags: [ builtin ]
---
# See also:
# - `alias` for listing or adding aliases

# This command is available in:
# - Bash
# - Zsh

# Remove all aliases:
unalias -a
# No output.

# Remove alias with specific names:
unalias <alias_name1 alias_name2 ...>
# Input example:
unalias egrep fgrep grep
# No output.

# Don't use aliases instead of functions. When possible prefer later ones.
